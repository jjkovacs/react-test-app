# react-test-app
First time trying to develop with React.

Built a small app to display a map of all of the counties in the US.

* Map Component - Leaflet
* Base Map Layer - ArcGIS Streets Night
* County Data - ArcGIS USA Counties Generalized Boundaries FeatureServer

## To Run:

Clone the repo.

Install the dependencies: `npm install`

Run the dev server: `npm start`